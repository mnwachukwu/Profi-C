# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
publish/
*.dll
*.exe
*.pdb

# Emitted Profi-C assemblies
*.pc.dll
*.pc.exe

# Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
coverage*.xml
coverage*.json

# Visual Studio / Rider / VS Code
.vs/
.vscode/*
!.vscode/extensions.json
!.vscode/tasks.json

# Colours for Profi-C source. Committed because they are about the language rather than
# about anyone's setup: a .pc file should read the same for whoever opens this repository.
!.vscode/settings.json
.idea/
*.user
*.suo
*.userprefs

# NuGet
*.nupkg
*.snupkg
packages/
!packages/build/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs and temp
*.log
*.tmp

# Editor and tool leftovers. Some in-place editors write the original beside the file they
# changed, and a stray copy of a source file is worse than useless: it compiles nowhere, and
# it reads as though someone meant to keep it.
*.bak
*.orig
*.rej
*~

# Local publish output, for putting profi-c and pc on PATH
dist/

# Installed by the editor grammar's tokenization test, which needs the engine
# VS Code itself uses. Run 'npm install' in editors/vscode to restore it.
node_modules/
package-lock.json
