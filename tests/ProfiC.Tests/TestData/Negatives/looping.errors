looping.pc(43,5): error PC0404: 'NothingEndsIt' yields an integer, but it can reach its end without yielding one. Yield on every path out.
looping.pc(44,9): opinion PC0406: Nothing here breaks, yields, or throws, so nothing will stop this loop. Add a 'break', or give it a condition with 'loop while' or 'until'.
looping.pc(54,13): error PC0243: 'amounts' is the sequence this 'loop each' is walking, and 'Insert' changes it. Collect the changes into another set, or count with a range loop.
looping.pc(65,13): error PC0243: 'amounts' is the sequence this 'loop each' is walking, and 'Remove' changes it. Collect the changes into another set, or count with a range loop.
looping.pc(80,13): error PC0243: 'amounts' is the sequence this 'loop each' is walking, and 'Clear' changes it. Collect the changes into another set, or count with a range loop.
looping.pc(92,27): error PC0316: 'loop each' needs a set or a string, and this is an integer. Ask it for one, or count with 'loop for'.
looping.pc(104,38): opinion PC0413: A step of zero leaves the counter where it started, so this loop runs forever. Give it a step that moves, or write 'loop' where running forever is meant.
looping.pc(119,9): warning PC0414: Counting from 1 by -1 never reaches 10, so nothing inside this loop runs. Check which way the step points.
looping.pc(129,9): warning PC0414: Counting from 5 by 1 never reaches 3, so nothing inside this loop runs. Check which way the step points.
looping.pc(140,9): warning PC0414: Counting from 5 by 1 never reaches 5, so nothing inside this loop runs. Check which way the step points.
looping.pc(153,29): error PC0316: 'loop each' needs a set or a string, and this is an integer[]?. Ask it for one, or count with 'loop for'.
