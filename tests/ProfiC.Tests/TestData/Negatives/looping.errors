looping.pc(32,5): error PC0404: 'NothingEndsIt' yields an integer, but it can reach its end without yielding one.
looping.pc(33,9): opinion PC0406: This loop has no condition, and nothing in it breaks, yields, or throws — so nothing written here will stop it. Add a 'break' where it should end, or write 'loop while' or 'until' and say the condition.
looping.pc(43,13): error PC0243: 'amounts' is the sequence this 'for each' is walking, and 'Insert' changes it. Its length was read when the loop began, so the walk would not follow. Collect what you want into another set and change that, or count with a range loop instead.
looping.pc(54,13): error PC0243: 'amounts' is the sequence this 'for each' is walking, and 'Remove' changes it. Its length was read when the loop began, so the walk would not follow. Collect what you want into another set and change that, or count with a range loop instead.
looping.pc(69,13): error PC0243: 'amounts' is the sequence this 'for each' is walking, and 'Clear' changes it. Its length was read when the loop began, so the walk would not follow. Collect what you want into another set and change that, or count with a range loop instead.
