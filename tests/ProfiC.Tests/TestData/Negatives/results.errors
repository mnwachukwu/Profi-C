results.pc(16,5): error PC0404: 'Describe' yields a string, but it can reach its end without yielding one. Yield on every path out.
results.pc(21,5): error PC0404: 'Grade' yields a string, but it can reach its end without yielding one. Yield on every path out.
results.pc(31,27): error PC0332: This produces no value, so there is nothing to use here.
results.pc(34,23): error PC0332: This produces no value, so there is nothing to use here.
